[package]
name = "mtrxai-protocol"
version = "0.2.1"
edition = "2021"
description = "Shared lobby wire types and peer ranking helpers for mtrxAI peer and server"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1.23.3", features = ["serde", "v4"] }
